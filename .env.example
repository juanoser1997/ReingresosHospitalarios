# Copiar como .env y completar los valores reales.
# El archivo .env NO debe commitearse a GitHub.

MODEL_NAME=diabetes-readmission-xgboost
MODEL_STAGE=Production
MLFLOW_TRACKING_URI=sqlite:///mlflow.db

# Para Docker Compose (cambiar el URI):
# MLFLOW_TRACKING_URI=http://mlflow:5000

# Optuna storage (SQLite local por defecto)
OPTUNA_STORAGE=sqlite:///optuna.db
